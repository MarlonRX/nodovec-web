---
import BaseLayout from "../layouts/BaseLayout.astro";
import { FeatureIcon } from "../components/reactComponents/FeatureIcon";
import { translate, getCurrentLanguage } from "../i18n";

export const prerender = false;

const lang = getCurrentLanguage();
const t = (key: string) => translate(key, lang);

// Middleware ya maneja los redirects, así que no necesitamos lógica aquí
---

<BaseLayout>
  <div class="w-full flex flex-col items-center justify-center px-4 pt-20 pb-20">
    <!-- Hero Section -->
    <div class="max-w-2xl text-center">
      <div class="inline-block px-4 py-2 bg-(--accent-primary)/10 border border-(--accent-primary)/30 rounded-full mb-6">
        <span class="text-sm font-semibold text-(--accent-primary) uppercase tracking-widest">{t("landing.welcomeTo")}</span>
      </div>

      <h1 class="text-6xl font-black text-(--text-primary) mb-4 tracking-tight">
        NodoVec
      </h1>

      <p class="text-xl text-(--text-secondary) mb-2 leading-relaxed">
        {t("landing.heroSubtitle")}
      </p>

      <p class="text-(--text-tertiary) mb-12">
        {t("landing.heroDescription")}
      </p>

      <!-- Features Grid -->
      <div class="grid grid-cols-3 gap-4 mb-12 py-8 border-y border-(--border-primary)">
        <div class="flex flex-col items-center gap-2">
          <div class="text-3xl text-(--accent-primary)">
            <FeatureIcon client:load icon="dashboard" size={32} />
          </div>
          <p class="text-sm font-bold text-(--text-secondary) uppercase">{t("landing.realTimeDashboard")}</p>
        </div>
        <div class="flex flex-col items-center gap-2">
          <div class="text-3xl text-(--accent-primary)">
            <FeatureIcon client:load icon="transactions" size={32} />
          </div>
          <p class="text-sm font-bold text-(--text-secondary) uppercase">{t("landing.trackTransactions")}</p>
        </div>
        <div class="flex flex-col items-center gap-2">
          <div class="text-3xl text-(--accent-primary)">
            <FeatureIcon client:load icon="analytics" size={32} />
          </div>
          <p class="text-sm font-bold text-(--text-secondary) uppercase">{t("landing.smartAnalytics")}</p>
        </div>
      </div>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <!-- Demo Button -->
        <a
          href="/demo"
          class="group flex items-center justify-center gap-2 px-8 py-4 bg-(--accent-primary) text-(--text-inverted) font-bold text-sm uppercase tracking-wider rounded-xl hover:bg-(--accent-hover) transition-colors shadow-lg hover:shadow-(--accent-primary)/30 hover:-translate-y-0.5 active:translate-y-0"
        >
          <span class="text-xl">
            <FeatureIcon client:load icon="demo" size={20} />
          </span>
          {t("landing.tryDemoFirst")}
          <span class="opacity-0 group-hover:opacity-100 transition-opacity">→</span>
        </a>

        <!-- Sign In Button -->
        <a
          href="/login"
          class="group flex items-center justify-center gap-2 px-8 py-4 bg-(--bg-secondary) text-(--text-primary) border-2 border-(--border-primary) font-bold text-sm uppercase tracking-wider rounded-xl hover:border-(--accent-primary) hover:text-(--accent-primary) transition-colors"
        >
          <span class="text-xl">
            <FeatureIcon client:load icon="security" size={20} />
          </span>
          {t("landing.signIn")}
          <span class="opacity-0 group-hover:opacity-100 transition-opacity">→</span>
        </a>
      </div>

      <!-- Sign Up Link -->
      <div class="mt-8">
        <p class="text-(--text-secondary) text-sm">
          {t("landing.newToNodoVec")}
          <a href="/register" class="text-(--accent-primary) font-bold hover:underline">
            {t("landing.createAccount")}
          </a>
        </p>
      </div>
    </div>

    <!-- Features Section -->
    <div class="max-w-4xl w-full mt-20 pt-20 border-t border-(--border-primary)">
      <h2 class="text-3xl font-black text-(--text-primary) text-center mb-12 uppercase tracking-tight">
        {t("landing.whyChoose")}
      </h2>

      <div class="grid md:grid-cols-2 gap-8">
        <div class="p-6 bg-(--bg-secondary) rounded-xl border border-(--border-primary)">
          <div class="text-4xl mb-3 text-(--accent-primary)">
            <FeatureIcon client:load icon="lightbulb" size={40} />
          </div>
          <h3 class="font-black text-(--text-primary) mb-2 uppercase">{t("landing.smartCategorization")}</h3>
          <p class="text-(--text-secondary) text-sm">
            {t("landing.smartCategorizationDesc")}
          </p>
        </div>

        <div class="p-6 bg-(--bg-secondary) rounded-xl border border-(--border-primary)">
          <div class="text-4xl mb-3 text-(--accent-primary)">
            <FeatureIcon client:load icon="target" size={40} />
          </div>
          <h3 class="font-black text-(--text-primary) mb-2 uppercase">{t("landing.budgetControl")}</h3>
          <p class="text-(--text-secondary) text-sm">
            {t("landing.budgetControlDesc")}
          </p>
        </div>

        <div class="p-6 bg-(--bg-secondary) rounded-xl border border-(--border-primary)">
          <div class="text-4xl mb-3 text-(--accent-primary)">
            <FeatureIcon client:load icon="mobile" size={40} />
          </div>
          <h3 class="font-black text-(--text-primary) mb-2 uppercase">{t("landing.responsiveDesign")}</h3>
          <p class="text-(--text-secondary) text-sm">
            {t("landing.responsiveDesignDesc")}
          </p>
        </div>

        <div class="p-6 bg-(--bg-secondary) rounded-xl border border-(--border-primary)">
          <div class="text-4xl mb-3 text-(--accent-primary)">
            <FeatureIcon client:load icon="secure" size={40} />
          </div>
          <h3 class="font-black text-(--text-primary) mb-2 uppercase">{t("landing.securePrivate")}</h3>
          <p class="text-(--text-secondary) text-sm">
            {t("landing.securePrivateDesc")}
          </p>
        </div>
      </div>
    </div>

    <!-- Demo Mode Info -->
    <div class="max-w-2xl w-full mt-20 p-8 bg-(--accent-primary)/5 border-2 border-(--accent-primary)/15 rounded-2xl">
      <h3 class="font-black text-(--text-primary) mb-3 uppercase tracking-tight flex items-center gap-2">
        <FeatureIcon client:load icon="demo" size={24}/>
        {t("landing.tryDemoMode")}
      </h3>
      <p class="text-(--text-secondary) text-sm mb-4">
        {t("landing.demoInfoDesc")}
      </p>
      <p class="text-(--text-tertiary) text-xs">
        {t("landing.demoReadonlyNote")}
      </p>
    </div>

    <!-- Footer -->
    <div class="max-w-2xl w-full mt-20 pt-8 border-t border-(--border-primary) text-center">
      <p class="text-(--text-tertiary) text-xs uppercase tracking-widest mb-4">
        {t("landing.madeWith", { heart: t("landing.heart") })}
      </p>
      <div class="flex justify-center gap-4 text-sm">
        <a href="#" class="text-(--text-secondary) hover:text-(--accent-primary) transition-colors">{t("landing.privacy")}</a>
        <span class="text-(--border-primary)">•</span>
        <a href="#" class="text-(--text-secondary) hover:text-(--accent-primary) transition-colors">{t("landing.terms")}</a>
        <span class="text-(--border-primary)">•</span>
        <a href="#" class="text-(--text-secondary) hover:text-(--accent-primary) transition-colors">{t("landing.support")}</a>
      </div>
    </div>
  </div>
</BaseLayout>
